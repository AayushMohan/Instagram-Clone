import React, { useEffect } from "react";
import faker from "faker";

const Stories = () => {
  useEffect(() => {}, []);

  return (
    <div>
      {/* Story */}
      {/* Story */}
      {/* Story */}
      {/* Story */}
      {/* Story */}
    </div>
  );
};

export default Stories;
